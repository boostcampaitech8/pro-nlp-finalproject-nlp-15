# RAG Configuration

# Chunking defaults
default:
  chunk_size: 1000
  chunk_overlap: 200

# Type overrides
overrides:
  textbook:
    chunk_size: 1500
  report:
    chunk_size: 800

# Vector DB
vector_db:
  url: "http://lori2mai11ya.asuscomm.com:6333"
  knowledge_base_collection: "knowledge_base_yejoon"
  event_collection: "events_yejoon"

# Embedding models
embedding:
  dense_model: "telepix/PIXIE-Rune-Preview"
  sparse_model: "telepix/PIXIE-Splade-Preview"
