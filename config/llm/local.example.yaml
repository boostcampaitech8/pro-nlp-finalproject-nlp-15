# LLM 설정 예시 - Local Server
# 이 파일을 복사하여 local.yaml을 만드세요
# cp config/llm/local.example.yaml config/llm/local.yaml

api_key: "YOUR_API_KEY"  # EMPTY인 경우 "EMPTY"
model: "your_model_name"
base_url: "http://your_server_ip:port/v1"

# 생성 파라미터
temperature: 0.7
max_tokens: 8192
